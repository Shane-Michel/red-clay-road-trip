# Copy this file to .env and update the values to match your environment.
# The application requires an OpenAI API key to generate road trip itineraries.
OPENAI_API_KEY=your-openai-api-key
# Optional: override the default model used for itinerary generation.
OPENAI_MODEL=gpt-4.1-nano
# Required for live place lookups.
OPENTRIPMAP_API_KEY=your-opentripmap-api-key
# Optional: enrich live data with TripAdvisor details.
TRIPADVISOR_API_KEY=
# Optional: override the default origin/referer sent to TripAdvisor APIs.
TRIPADVISOR_ALLOWED_ORIGIN=https://rcrt.redclayfieldguide.com
# Optional: enrich live results with Yelp business data.
YELP_API_KEY=
# Optional: include real-time weather from OpenWeather.
OPENWEATHER_API_KEY=
